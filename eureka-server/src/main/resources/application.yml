server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:8888/eureka'
    fetch-registry: false
    register-with-eureka: false
  instance:
    hostname: localhost
spring:
  application:
    name: eureka-server

